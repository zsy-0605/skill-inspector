interface:
  display_name: "Skill Inspector"
  short_description: "Preflight environment, package, and capability checks"
  default_prompt: "Use $skill-inspector to check whether this Agent Skill can run in my current environment."
