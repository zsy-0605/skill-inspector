{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Agent Skill compatibility benchmark predictions",
  "type": "object",
  "required": ["schemaVersion", "datasetVersion", "method", "model", "run", "environment", "skills"],
  "properties": {
    "schemaVersion": {"const": "1.1"},
    "datasetVersion": {"type": "string"},
    "method": {"enum": ["AGENT_ONLY", "JAVA_STATIC", "AGENT_WITH_INSPECTOR"]},
    "model": {"type": "string"},
    "run": {"type": "integer", "minimum": 1},
    "environment": {"type": "string"},
    "skills": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "readiness", "dependencies"],
        "properties": {
          "id": {"type": "string"},
          "readiness": {"enum": ["READY", "WARNING", "NOT_READY", "UNVERIFIABLE"]},
          "dependencies": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["type", "name", "inScope", "evidence"],
              "properties": {
                "type": {"type": "string"}, "ecosystem": {"enum": ["python", "npm", "maven"]}, "name": {"type": "string"},
                "required": {"type": "string"},
                "inScope": {"type": "boolean"}, "evidence": {"type": "string"},
                "necessity": {"enum": ["REQUIRED", "OPTIONAL", "CONDITIONAL"]},
                "source": {"enum": ["DECLARED", "INFERRED", "JAVA_REPORT"]},
                "confidence": {"enum": ["CERTAIN", "HIGH", "MEDIUM", "LOW"]}
              },
              "allOf": [{"if": {"properties": {"type": {"const": "package"}}}, "then": {"required": ["ecosystem"]}}]
            }
          }
        }
      }
    }
  }
}
